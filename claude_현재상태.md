# 고객상담관리 앱 — 작업 현황 메모 (2026-09-04 기준)

새 대화를 여셨다면 이 메모부터 참고하면 지금까지 상황을 빠르게 파악할 수 있어요.

## ⭐ 새 세션 Claude가 먼저 알아야 할 핵심 4가지
1. **카카오 설정 완료** — 앱 ID 1559026, JS키 `8e104d55c5268f324f7aa0319e3cddbb`(코드 하드코딩), 도메인(`jam-customer-console.pages.dev`) 등록, REST키는 Cloudflare 환경변수 `KAKAO_REST_KEY`(Secret)에 설정 완료. 지도·소요시간 정상 작동 확인됨.
2. **git 잠금 이슈** — 이 코워크 마운트는 파일 삭제(unlink)가 막혀 커밋 때 생기는 `.git/index.lock`이 자동으로 안 지워짐. → 커밋 전 lock을 `mv`(이름변경)로 치우고 커밋할 것. 자세한 방법은 아래 "git 잠금" 항목.
3. **같은 폴더 병행 작업 금지** — 다른 Claude 세션·툴이 같은 폴더를 동시에 만지면 커밋 충돌 위험. 한 곳에서만 작업할 것.
4. **세션 시작하자마자 `git fetch origin && git status --short --branch`부터 실행할 것** — 2026-09-03에 로컬 저장소 역사가 원격과 완전히 끊어진 사고(고아 커밋)가 한 번 있었고, 그때 핵심 파일 3개(index.html·style.css·core.js)가 통째로 사라진 적이 있음(원인 불명, jam님 컴퓨터 쪽 문제로 추정 — 확실치 않음). `ahead/behind`가 비정상적으로 크거나 `Unable to merge unrelated histories` 에러가 나면, 바로 아래 "2026-09-03 저장소 손상 사고" 항목부터 읽고 진단할 것. 지금(2026-09-04)은 로컬=원격 완전 동기화 상태.
5. **버전 번호를 올릴 때마다 index.html의 `?v=NN` 캐시버스터도 같이 올릴 것(2026-09-07 추가)** — `public/index.html`의 `<link>`·`<script>` 태그에 `?v=44` 식으로 버전 번호가 붙어있다. 코드를 고쳐도 이 숫자를 안 올리면 브라우저가 예전 파일을 계속 캐시해서 써버려, 배포는 됐는데 "그대로예요"라는 혼란스러운 보고가 나올 수 있다(2026-09-07에 실제로 이 문제로 몇 차례 헛수고함). 홈 화면 하단 부제(`v44 (날짜)`)가 지금 jam님이 보고 있는 화면의 실제 버전이니, "고쳤는데 그대로다"라는 보고를 받으면 제일 먼저 "홈 화면에 몇 번(vNN)이라고 뜨나요?"부터 물어볼 것 — 새 버전 번호가 안 보이면 아직 배포가 안 됐거나(푸시 안 함) 캐시 문제다.

## 앱 개요
- 이름: jam-customer-console (보험영업용 고객상담관리 앱)
- 구조: Cloudflare Pages(정적 파일 + Functions) + Cloudflare D1(공용 DB) + Cloudflare R2(사진·음성 파일 저장) + 브라우저 IndexedDB(기기별 캐시, 클라우드와 동기화)
- 저장 위치(사용자 PC): `D:\푸본현대생명\jam앱\jam-customer-console\`
- 배포: GitHub 저장소에 푸시하면 Cloudflare Pages가 자동 배포. **코드를 고쳐도 GitHub Desktop에서 커밋·푸시해야 실제 사이트에 반영됨.**

## 폴더 구조 (2026-08-23 확인)
```
jam-customer-console/
├── .git/                    (GitHub 연동·버전 기록 — 절대 삭제 금지)
├── db/schema.sql            (DB 구조 정의)
├── docs/data-schema.md      (데이터 구조 설명)
├── functions/
│   ├── _lib/advisors.js     (담당자 인증)
│   ├── _lib/prompts.js      (AI 프롬프트 모음)
│   └── api/
│       ├── analyze.js       (AI 정리·분석 — Whisper 음성인식도 여기 있음)
│       ├── data.js          (텍스트 데이터 저장, D1)
│       └── files.js         (사진·음성 파일 저장, R2)
├── public/
│   ├── css/style.css
│   ├── js/ (advisor, analysis, ap, core, customers, filesync, ocr, pools, voice)
│   └── index.html           (메인 화면)
└── README.md
```

## AI가 실제로 어떻게 쓰이는지 (중요 — 자주 헷갈림)
- **D1 = 텍스트 저장 창고, R2 = 파일 저장 창고. 둘 다 AI가 아님.**
- **"AI로 정리"(상담사례풀 등) → Claude Haiku 모델(Anthropic, 유료).** Claude 중 제일 저렴한 급.
- **"보장 분석"·"가입설계 분석" → Claude Sonnet 모델(Anthropic, 유료).**
- **음성인식(Whisper)만 예외 → Cloudflare Workers AI(무료 토큰).**
- 처음엔 정리·분석도 Cloudflare 무료 AI로 하려 했으나, 한국어·보험 용어 품질이 떨어져서 **Claude(유료)로 결정. 이 방침 유지(A안 확정, 2026-08-23).**
- 비용 절감 장치: 개조식 핵심내용(`keyContent`)을 미리 압축해두고, 프롬프트 캐싱을 사용. 정리를 한 번 잘 해두면 이후 분석마다 드는 토큰이 줄어듦.

## voice.js 는 Whisper 와 무관 (헷갈리지 말 것)
- `public/js/voice.js` = 브라우저 자체 음성기능(TTS 읽어주기 + STT 실시간 받아쓰기).
- 용도: 고객 이름 불러서 검색, 고객 정보를 말로 입력. **녹음 파일을 글로 바꾸는 Whisper와는 다른 것.**
- Whisper 음성인식은 `functions/api/analyze.js`의 `organize_pool` 모드에 있음.

## 앱8 세션에서 완료한 작업
1. 고객 검색창 한글(IME) 입력 깨짐 버그 해결.
2. AI 버튼 4개(정리·분석, 상담사례 매칭, 가입설계 분석, 상담 멘트 생성)를 보라색 3D 버튼(`.btn-ai`)으로 구분.
3. "참조풀" 화면 전면 개편 — 관리자 화면(⚙ 관리자 화면 → 참조풀 관리)에서만 자료 관리:
   - 텍스트(.txt)·음원 파일 드래그&드롭(여러 개 동시)
   - "AI로 정리" → 제목·요약(목차식)·핵심내용(개조식·4000자)·태그 자동 생성
   - PDF·이미지는 별도 칸에서 보여주기 전용(AI 미분석)
   - "AI로 정리"는 관리자 비밀번호로 바로 인증
   - 담당자 화면에서는 참조풀 항목을 읽기 전용으로 조회
4. AI 정리 시 분석에 쓰이는 값은 `keyContent`(핵심내용) — 미리 압축해 비용 절감.
5. "AI로 정리" 실패 시 영문 오류를 한국어 메시지로 개선. 음원 4MB↑ 미리 경고.
6. "AI로 정리·분석"과 "가입설계 분석"은 사진·PDF를 실제로 AI가 보고 분석. 참조풀 ④번 칸은 의도적 미분석.

## 앱9 세션에서 확인·결정·작업한 것 (2026-08-23)

### 확인·결정
1. **files.js 두 개 정리** — 정상 버전은 `functions/api/files.js`(첫 줄 `// functions/api/files.js`, 경로 `../_lib/`, R2 저장). 루트에 있던 `files.js`는 "System initialization" 대화에서 나온 우회 패치 버전(경로 `../lib/` 틀림, 설명글 섞임)으로 **앱에서 안 쓰는 파일 → 지워도 됨.** (500 에러는 정상 버전으로 이미 해결 완료.)
2. **음성인식(Whisper) 큰 파일 한계 확인** — Cloudflare Whisper는 1~2MB만 넘어도 실패하거나 결과가 깨짐. 40분 음원이 되는 것처럼 보였던 건 착각(음원+텍스트를 같이 넣었을 때 텍스트만 처리된 것). **긴 상담 녹음은 앱 내장 Whisper로 불가능.**
3. **긴 상담 녹음 최종 방법 = 삼성 갤럭시 "음성 녹음" 앱 사용.** (다글로·클로바노트 다 불필요)
   - 삼성 음성녹음이 녹음 후 자동으로 텍스트 변환 + 음원/텍스트 파일 다운로드 제공.
   - 최종 흐름: **삼성 음성녹음으로 텍스트 뽑기 → 관리자 창 상담사례풀에 붙여넣기 → "AI로 정리"(Claude Haiku) → D1 저장 → 이후 다른 고객 분석에 참고자료로 활용(A안: 공용 상담사례풀에 축적).**
4. **Claude는 음성 파일을 직접 못 들음(2026 기준)** — 텍스트·이미지만 처리. 그래서 "음원+텍스트를 같이 AI에" 방식은 우리 앱(Claude)에선 불가능. 반드시 텍스트로 바꾼 뒤 넣어야 함.
5. **정리·분석 AI = Claude 유지(A안 재확정)** — 무료 Cloudflare AI는 한국어·보험 용어 품질이 떨어져서 안 씀.

### 실제로 코드를 고친 것 (배포하려면 GitHub 푸시 필요)
6. **prompts.js — 상담사례풀 정리 오류 방지 규칙 추가**: 음성변환 텍스트의 오탈자·오인식(회사명·숫자·용어)을 추측하지 말고 "(확인 필요)"로 표시. (organizePoolSystem)
7. **advisor.js — 관리자 참조풀 "새 상담사례" 화면 개선**:
   - ①번 칸은 텍스트(.txt)만 드롭·업로드. 음원 드롭하면 안내 후 무시.
   - 음원은 아래 음원 칸에서 올려 "듣기 전용"(AI 정리에 안 보냄).
   - 텍스트 파일 넣으면 본문에 `━━━ 📄 파일이름 ━━━` 구분선과 함께 삽입.
   - 파일 목록 각 칩에 **× 삭제 버튼** — 누르면 그 파일 블록만 본문에서 제거(파일별 정확 삭제). 본문을 손으로 고쳐 구분선이 사라지면 안내만.
8. **이미지 분석 정확도 개선 (핵심 원인 2가지 해결)**:
   - **원인 A: 이미지 이중 압축** — 저장할 때(1500px/0.72)와 AI 전송할 때(1500px/0.72) 두 번 JPEG 압축돼 글자·숫자가 뭉개짐. → **1568px(Anthropic 권장 최적값), 저장 품질 0.9 / 전송 품질 0.92로 상향.** (`ocr.js` 보장분석 3곳, `analysis.js` 가입설계 2곳)
   - **원인 B: 프롬프트가 바로 압축만 시킴** — AI가 사진을 충분히 보기 전에 값부터 채움. → tidySystem·planSystem에 **"결과 쓰기 전에 사진을 한 장씩 정독, 숫자 자릿수·쉼표 주의" 단계 추가.** (`prompts.js`)
   - (참고) 버튼별 모델 연결은 점검 결과 모두 적절 — 이미지 분석(tidy/plan/analyze)은 Sonnet, 텍스트 정리(organize_pool/ap/summarize)는 Haiku. JSON 형식 자체는 이미 사용 중이라 정확도와 무관.

### 이번에 수정된 파일 (덮어쓸 위치)
- `functions/_lib/prompts.js` ← 상담사례풀 규칙 + 보장분석/가입설계 정독 단계 (셋 다 합쳐진 최신본)
- `public/js/advisor.js` ← 상담사례 화면 텍스트만/음원 듣기전용/파일 X 삭제
- `public/js/ocr.js` ← 보장분석 이미지 해상도·품질 상향
- `public/js/analysis.js` ← 가입설계 이미지 해상도·품질 상향

## 앱10 세션에서 한 것 (2026-08-28) — 기능 대거 추가

### 음성인식(Whisper)
- 음원 → AI 받아쓰기 "다시 켬": 관리자 참조풀 "AI로 정리" 때 첨부 음원을 Whisper로 받아쓰기해 함께 정리(앱9에서 듣기전용으로 껐던 걸 되돌림). `advisor.js organizeAdminPool`.
- 모델 신형 교체: `@cf/openai/whisper` → `@cf/openai/whisper-large-v3-turbo`(base64 직접 전달, 더 긴 음원 가능). 20MB↑ 서버 차단. `analyze.js organize_pool`.
- 크기 경고(5MB soft/20MB block)·실패 경고·성공 토스트 추가. 긴 상담녹음은 여전히 휴대폰 음성녹음 권장(짧은 음원용).
- 전제: Cloudflare Pages에 Workers AI 바인딩(변수명 `AI`) — 설정 완료 확인함.

### 가입설계 — 사전심사 칸
- 가입설계 이미지 위에 "사전심사(질병·부담보)" textarea(`c.planPreScreen`). "가입설계 분석" 시 설계서 이미지 + 사전심사 내용 함께 분석. `analysis.js`, `analyze.js plan`, `prompts.js planSystem`.

### 고객 필드 추가 + 프롬프트 반영
- 전화번호(phone, eb4c9d7). 이름 우측 성별(`c-gender`), 전화 우측 직업(`c-job`), 지역 아래 주소(`c-address`)+상세주소(`c-address-detail`).
- 주소 찾기 = 카카오 우편번호 서비스(무료·키불필요). `openAddrSearch`/`openAddrSearchInto`.
- **성별·직업을 보장분석·가입설계·상담멘트(analyze/plan/ap) 프롬프트에 반영. 성별 미상 시 남성 가정 금지 규칙 추가.** (전엔 성별이 없어 AI가 남성으로 가정하던 문제 해결)
- 마스킹: 전화·주소·주민번호를 평소 가리고 hover/터치로 보기(오버레이 방식, 저장은 항상 원본). `mask-wrap`/`mask-ov`.
- 주민번호는 한 칸(`c-rrn`)으로 통합, **`000000-0******`** 표시(앞6+성별1자리만 보임). 앞6으로 만나이 자동계산. 저장 시 birth6/rrnBack로 분리 보관. `onRrn`.
- ⚠️ 주민번호 뒷자리(`rrnBack`) 평문 저장 — 민감정보 취급 주의. `docs/data-schema.md`에 문서화.

### 지도 · 자동차 소요시간 (신규)
- 고객 주소칸 아래 "🗺️ 지도로 찾기" → **전체화면** 지도(시트 아님). `#ov-map`, `openMapSheet`/`closeMapFull`.
- 카카오맵 SDK(services)로 지도·주소→좌표(지오코딩)+역/건물명 키워드 폴백(`_geocode`).
- 출발/도착/경유지(＋경유지 추가) 각각 주소 찾기 버튼. 구간별 시간·거리 표시 + **구간마다 다른 색 경로선(polyline)**.
- 자동차 경로 = Cloudflare Function `functions/api/directions.js`(카카오모빌리티 여러경유지 API, env `KAKAO_REST_KEY` 필요).
- 카카오내비 버튼: JS SDK로 **도착지 안내**. 경유지는 웹 연동 불가(카카오 미지원) → 도착지만(사용자와 1번안 확정).

### 카카오 개발자 설정 (완료)
- 카카오 앱: **jam-customer-console (ID 1559026)**. JavaScript 키 `8e104d55c5268f324f7aa0319e3cddbb`(코드에 하드코딩, 공개용·도메인제한).
- **플랫폼 키 → JavaScript 키 → 수정 → "JavaScript SDK 도메인"**에 `https://jam-customer-console.pages.dev` 등록(← 지도 뜨는 핵심). 카카오맵 사용 ON.
- REST API 키는 Cloudflare 환경변수 **`KAKAO_REST_KEY`(Secret)**에 넣음(자동차 소요시간용) — 설정 완료.

### ⚠️ 알아둘 것 (git 잠금)
- 이 코워크 마운트는 파일 삭제(unlink)가 막혀 있어, 커밋 때 생기는 `.git/index.lock`·`HEAD.lock`이 자동으로 안 지워져 다음 커밋을 막음. → 커밋 전 lock을 이름변경(mv)으로 치우고 커밋하는 방식으로 처리해 왔음. `.git`에 `*.disabled`·`trash.*`·`.old` 찌꺼기 쌓여도 git엔 무해. GitHub Desktop이 "another process running" 뜨면 `.git\index.lock` 파일 삭제하면 됨.

## 앱11 세션에서 한 것 (2026-08-28) — 배포 사이트 실측

배포 사이트(`jam-customer-console.pages.dev`, v40)에서 앱10 기능을 직접 눌러 테스트. **버그 없음, 코드 수정·커밋 없음(메모만 갱신).**

실측으로 정상 확인:
- **마스킹** — 전화 `010-****-5678`, 주민번호 `900101-2******`(앞6+성별자리만). 입력폼·고객상세 둘 다 마스킹됨.
- **주민번호 통합칸 + 만나이** — `900101-2******` 한 칸, 만나이 36세·연령대 30대 자동계산.
- **지도** — 전체화면, 경유지 추가, 구간별 시간(서울역→강남역 22분·10.7km / 강남역→판교역 20분·15.8km), 구간별 색 경로선(파랑·빨강), 카카오내비 버튼(콘솔 에러 없음). 카카오모빌리티 경로 API·지도 SDK 정상.
- **사전심사 칸** — 가입설계(③단계)에 칸 존재. 코드상 `preScreen`이 plan 프롬프트에 "반드시 반영"·부담보 미보장 처리로 연결됨(analysis.js→analyze.js→prompts.js 확인).

코드로 연결만 확인(유료/파일 필요해 라이브 미실행):
- **성별 여성 반영** — 보장분석(analysis.js:292)·가입설계(:337)·상담멘트(ap.js:137) 3곳 모두 `gender` 전송, 서버 프롬프트에 "성별: 여" 주입, 미상 시 중립 규칙(prompts.js). ⚠️ 실제 여성 기준 분석문은 실보장 이미지로 유료 실행해야 최종 확인.
- **음원 받아쓰기** — advisor.js 음원 칸→organizeAdminPool→base64, analyze.js `@cf/openai/whisper-large-v3-turbo`(base64 직접), 20MB 차단·실패 한국어 안내. ⚠️ 짧은 실음원으로 최종 확인 필요.

## 앱12 세션에서 한 것 (2026-08-29)
1. **홈 화면 "자료 준비"/"상담·분석" 모드 구분 폐지** — 실사용은 이어서 하는 흐름인데 모드에 따라 추가·이동 버튼이 숨겨져 오히려 방해였음. 이제 홈은 하나로 통합, 고객 삭제는 confirm() 경고로 안전 확보. `core.js enterCustomers`, `style.css`.
2. **매니저상담 1·2단계 화면 통합** — "상담사례매칭"과 "분석옵션+실행"으로 나뉘어 있던 걸 합침(그 이후 세션에서 이 구조를 계속 다듬음 — 아래 앱13 참고).
3. **삭제(×) 버튼 전부에 확인창(confirm) 추가** — 사진·음원 등 그동안 확인창 없이 바로 지워지던 버튼들 전체 점검·추가.
4. **AP 아이스브레이크 멘트에 "고객니즈 환기" 섹션 추가**.
5. **사진 등록 12장 하드 제한** — 보장자료·가입설계서 이미지 모두, AI가 실제로 분석에 반영하는 장수(12장)와 등록 가능 장수를 일치시킴. 그 이상 등록하면 안내 후 막음.
6. **보장분석·가입설계 프롬프트에 5000자 출력제한 추가** — 8000토큰에서 결과가 잘리던 문제 방지.
7. **`docs/AI기능_상세리포트.pdf` 생성** — AI 기능 전체를 정리한 리포트. 이후 앱13에서 "9. Claude Skill 제안" 섹션을 추가하며 두 번 더 개정(`a453f75`, `21514ba`).

## 앱13 세션에서 한 것 (2026-09-01 ~ 09-04) — 스킬 정리 + UI/UX 다수 개편 + 저장소 사고 복구

### 스킬(Claude Skill) 정리
- 11개 세션에 걸쳐 jam님이 모아온 두 원본 자료(`SKILL_jam작업방식.md`, `AI기능_상세리포트_클로드스킬.docx`)를 읽고, 개조식으로 정리한 `skill/skill_jam2.md` 작성.
- 실제 Claude Skill 두 개(`jam-dev-collaboration-style`, `jam-ai-feature-collaboration`)를 최신 내용으로 업데이트(옛날 환경 가정 — SendUserFile·GitHub 웹업로드 등 — 다 제거하고, 현재 방식인 "Claude가 로컬 커밋, jam님이 GitHub Desktop으로 Push"로 정정).

### ⚠️ 2026-09-03 저장소 손상 사고 (참고용 — 복구 완료, 재발 시 대응법)
- jam님 컴퓨터에서 `public/index.html`·`public/css/style.css`·`public/js/core.js` 3개 파일이 통째로 사라지고, 그 상태로 GitHub Desktop이 기존 역사와 연결 안 된 새 "고아 커밋"(수정2)을 만든 적이 있음. `git pull`이 `Unable to merge unrelated histories` 에러로 실패.
- 원인은 끝까지 알아내지 못함(추정하지 않음 — 모름).
- 복구 방법: `git fetch origin`으로 원격의 온전한 버전을 확인 → 로컬 파일 내용이 원격과 실제로 다른지 `git diff origin/main`으로 대조 → 다른 게 없으면(사라진 파일 3개만 없는 상태) `git reset origin/main`(mixed reset, 워킹트리는 안 건드림)으로 index·HEAD만 원격에 맞추고, 이 코워크 환경은 파일 unlink가 막혀 있어 기존 파일을 지우고 새로 쓰는 `git reset --hard`는 실패할 수 있음 — 그럴 땐 대상 파일들을 먼저 `mv 파일 파일.bak.타임스탬프`로 치운 뒤 재시도.
- 지금(2026-09-04)은 로컬=원격 완전 동기화 확인됨. 앞으로 세션 시작할 때 `git fetch && git status --short --branch`로 먼저 상태 확인하는 습관 들이기(위 핵심 4번 참고).

### UI/UX 조언 후 실제 반영
- **모바일/PC·태블릿 수동 선택 버튼 제거** — 로그인 화면의 "📱 모바일 / 💻 PC·태블릿" 버튼과 그 localStorage 저장값을 없애고, 화면 폭 561px 기준 순수 CSS 미디어쿼리 반응형으로 전환. `index.html`, `js/core.js`, `css/style.css`.
- **버튼 색 통일** — `primary`와 `btn-blue`가 육안으로 안 구분돼서 같은 파란색으로 통일(마크업 클래스명은 안 건드리고 CSS 색상만 통일).
- **스플래시(로딩) 화면 색을 홈 배너와 같은 청록~초록 톤으로 통일**.

### 참조풀
- **상품별 섹션 방식으로 전면 개편** — 예전엔 필터 칩을 눌러야 그 상품만 보였는데, 이제 들어가자마자 종신·정기·암·건강 등 상품별로 섹션(묶음)으로 나뉘어 보임. 상품 태그 없는 항목은 "미분류" 섹션. 한 항목이 상품 여러 개에 걸치면 해당 섹션 모두에 나옴. `pools.js renderPools`.
- **항목 상세보기에 "📋 복사" 버튼 추가** — 공용 서브페이지(`ov-subpage`) 하단에 복사 버튼을 추가해서 참조풀 항목 등 내용을 클립보드로 바로 복사 가능(`copySubPage()`).

### 매니저상담(분석) 화면
- **참조풀 매칭 로직 변경** — 담당자가 선택(☑)한 항목이 있으면 그것만 쓰고(자동매칭과 안 섞음), 선택한 게 없을 때만 태그 겹침 점수로 자동매칭하되 **최대 2개까지만**. `analysis.js relevantPool()` — 이 함수 하나를 고쳐서 "참조풀 매칭"·"참조 자료"·"참고 에피소드" 세 곳에 동시 적용됨.
- **보장분석 결과 팝업(`ov-anresult`)** — "가입설계 분석" 버튼을 상단에서 하단(닫기 버튼 옆, 나란히)으로 이동. 누르면 매니저상담 2단계 화면 맨 위부터 보이게(`anStepGo()`에 `window.scrollTo(0,0)` 추가).
- **매니저상담 1단계** — "보장분석 결과보기/실행" 버튼과 "가입설계"(예전 "다음: 가입설계 ›") 버튼을 상하 배치에서 좌우 나란히(`btn-grid`)로 변경. 가입설계 버튼 누르면 화면 상단부터 보임.
- **1·2단계 제목("① 상담사례 매칭·보장분석" / "② 가입설계") 글자 크게·굵게** 변경.
- (참고, 손 안 댐) "참조풀 매칭"과 "참조 자료" 두 섹션이 내부적으로 완전히 같은 데이터(`refPoolAll`)를 보여주는 중복 구조라는 걸 발견함 — 언젠가 정리하면 좋을 부분.

### AP(고객대면) 화면
- **"고객대면 이후 상담결과 입력" 화면 신규 추가** — AP를 저장하면 성공/보류/실패 + 다음 일정(날짜) + 메모를 입력하는 팝업(`ov-consult`)으로 이어짐. 저장하면 고객의 `seg`(방문예정/상담/계약)도 자동으로 맞춰줌(성공→계약, 그 외엔 상담 유지). 고객 목록 카드에 결과 뱃지·다음 일정도 표시. `ap.js openConsultResult/saveConsultResult`, `customers.js`.
- **저장 버튼 정리** — 예전엔 화면 상단(모든 단계에서 보임)과 5단계 하단, 두 곳에 똑같은 "저장" 버튼이 있어서 헷갈렸음. 상단 버튼은 없애고, 5단계 하단에 역할이 다른 버튼 2개로 재구성:
  - **"AP 저장하고 닫기"**: 저장 후 위 상담결과 입력 화면으로 이어짐.
  - **"저장만 하고 나가기"**: 저장만 하고 상담결과는 나중에 입력하기로 하고 바로 고객 목록으로.
  - 공통 저장 로직은 `persistAPData(c)`로 뽑아서 두 버튼이 같이 씀.

### 논의만 하고 보류한 것 (다음에 다시 얘기하기로 함)
- **홈 화면 하단 탭 구성 변경** — jam님이 "고객-장소-분석-상담-고객관리"라고 언급했는데, 지금 4개 탭(고객/참조풀/분석/상담)과 다르고 "장소"·"고객관리"가 정확히 뭘 뜻하는지 아직 확인 안 됨. **다음 세션에서 이 부분부터 구체적으로 여쭤볼 것.**

### 다른 프로젝트(jam-work-growth, FP 업무·성장 플랫폼) — 별도 저장소
- 이번 세션에서 이 프로젝트도 잠깐 작업함: "Today's insight"의 "AI로 사진 만들기"가 항상 "사무실 인물 사진"만 나오던 걸, 사무공간/인물/빈티지 카드/미니멀 카드/흑백 사진 5가지 스타일 중 매번 무작위로 고르도록 변경(`src/routes/ai.js` `PHOTO_STYLES`). 자세한 내용은 그 저장소 쪽에 별도로 메모해둘 것(이 문서는 jam-customer-console 전용).

## 앱14 세션에서 한 것 (2026-09-05)
1. **"참조풀 매칭"/"참조 자료" 중복 섹션 정리 완료** — 매니저상담 1단계에서 완전히 같은 데이터(`refPoolAll(c)`)를 이름만 다르게 두 번 보여주던 문제 확인 후, jam님 확인받아 아래쪽 "참조 자료" 섹션 삭제. "참조풀 매칭" 하나만 남음. `analysis.js` (커밋 `0d86396`, **Push 필요**).
2. **유료 실측 2건(음원 받아쓰기·여성 보장분석) — jam님이 "없는 걸로" 결정, 할 일 목록에서 제외.**
3. **테스트고객(`mtbnuaiasw995`) — jam님이 필요할 때 직접 삭제하기로 함. 할 일 아님, 참고용으로만 남김.**
4. **홈화면 6탭 구조 설계 논의 + 1·2단계 구현** — TA-고객정보-참조풀-분석-상담-고객관리 6탭으로 재배치, 홈은 하단 탭에서 빼서 상단 우측 아이콘으로 이동(1단계). TA 관리자 화면 + 담당자 읽기화면도 구현(2단계). 자세한 설계·UX의견·남은 결정사항은 `docs/6탭구조_설계메모.md` 참고.
   - 1단계 코드: `index.html`(탭바 6개로 교체, `s-ta`·`s-customermgmt` 화면 자리 추가), `core.js`(`header()` 홈버튼 우측 이동, `go()`에 ta/customermgmt 라우팅), `style.css`(탭 라벨 폭 조정, 홈 버튼 크기 19px→27px로 확대).
   - 2단계 코드: `advisor.js`(관리자 화면에 "TA 관리" 탭 추가 — 참조풀과 완전히 별개인 `poolType:'ta'`로 저장, 같은 자료 편집기·AI정리·이미지/음원 기능을 재사용하되 "참조풀 관리" 화면의 종류탭에는 안 섞이게 분리), `core.js`(`renderTA` — 담당자 화면에서 TA 자료를 읽기 전용 카드로 목록 표시, 상세보기는 참조풀과 같은 `viewPoolItem()` 재사용).
   - 고객관리(3·4단계, 히스토리·Q&A)는 아직 "준비중" 빈 화면만 있음.

5. **홈 버튼 하단 탭과 크기 통일 + 글자 라벨 추가** — 상단 우측 홈 아이콘이 하단 탭(18px)보다 작아 보인다는 피드백으로, 아이콘 크기를 하단과 똑같이 맞추고 "홈" 글자를 아이콘 아래에 추가(하단 탭과 완전히 같은 모양).
6. **6탭 구조 3단계: 고객관리 히스토리 완료** — `public/js/customermgmt.js` 신설.
   - 고객관리 탭에서 고객을 고르면, 메모(텍스트) 입력 또는 음원(드래그&드롭·"📁 폴더에서 선택") 업로드 후 "🤖 AI로 정리해서 히스토리에 추가" 버튼으로 정리 → 고객별 히스토리 타임라인에 쌓임(항목 클릭 시 상세보기+삭제).
   - 서버에 새 모드 `organize_history` 추가(`functions/api/analyze.js`, `functions/_lib/prompts.js`) — 참조풀 정리(`organize_pool`, 관리자 비밀번호 인증)와 달리 이건 담당자 개인 고객 자료라 **일반 담당자 로그인**으로 인증되게 분리함.
   - AP 상담결과 저장(`ap.js saveConsultResult`) 시 자동으로 히스토리에 한 줄(상담결과+메모+다음일정) 추가됨.
   - "☁️ 구글드라이브에서 가져오기" 버튼은 있지만 아직 실제 연동 안 됨(OAuth 설정 필요) — 누르면 안내 문구만 뜸.
7. **구글 드라이브 실제 연동 완료** — jam님이 Google Cloud Console에서 API키·OAuth 클라이언트ID·앱ID 직접 발급(`docs/구글드라이브_연동_설정가이드.md`에 값·과정 정리해둠). "☁️ 구글드라이브에서 가져오기" 누르면 실제 구글 파일선택창(Picker)이 뜨고, 고른 파일을 Drive API로 내려받아 음원 칸에 자동 첨부됨. `drive.file` 스코프라 사용자가 직접 고른 파일에만 접근(드라이브 전체 열람 아님).
8. **6탭 구조 4단계: 고객관리 Q&A 코너 완료 — 6탭 개편 전체 완성.** 고객관리 화면에 "🤖 이 고객 히스토리로 Q&A 물어보기" 버튼 추가. 서버에 새 모드 `history_qna` 추가(organize_history와 같은 방식으로 일반 담당자 인증). 저장된 히스토리의 `summary`(요약)만 근거로 답하고, 기록에 없는 내용은 "확인 안 됨"이라고 답하도록 프롬프트에 명시. 비용 절감: 원문(rawText)은 안 보내고 최근 항목 위주로 6000자 예산 안에서만 사용.

## 앱15 세션에서 한 것 (2026-09-06) — 6탭 실사용 피드백 6건 반영
jam님이 실제로 써보고 준 피드백 6개, 전부 반영 완료.
1. **홈 버튼이 여전히 작음** — 하단탭과 크기를 맞췄는데도 작다는 피드백. 이번엔 하단탭보다 확실히 크게(18px→26px, 라벨도 10.5px→12px+굵게) 키움. `style.css`.
2. **홈화면 "시작하기"가 고객화면으로 감** — 6탭 흐름상 시작은 TA부터라, `enterCustomers()`가 `go('ta')`로 가게 수정. 홈화면 카드의 아이콘·설명 문구도 TA→고객정보→고객관리 흐름으로 갱신. `core.js`, `index.html`.
3. **TA 화면에 다음 버튼 없음** — TA 화면 맨 아래 "다음: 고객정보 →" 버튼 추가(자료 없을 때도 뜸). `core.js`.
4. **상담사례풀 분류 안 됨** — 원인: 관리자 화면에서 저장할 때 태그가 전부 `free`로만 들어가고 `product` 필드는 항상 비어있었음(예전 "분류창" UI는 이미 아무도 안 쓰는 죽은 코드였음). 저장 로직을 고치는 대신 **표시하는 순간에** `product`/`situation`/`age`/`free` 어디에 태그가 있든 8개 상품군과 부분일치로 찾아 분류하도록 변경 — 기존에 이미 "미분류"로 쌓인 자료도 마이그레이션 없이 바로 재분류됨. `pools.js` (`matchedProducts()` 추가).
5. **AP 저장 버튼 정리** — "AP 저장하고 닫기"를 "✓ 저장하고 고객관리로 →"로 바꾸고, 상담결과(성공/보류/실패+다음일정+메모) 저장을 마치면 고객목록이 아니라 그 고객의 **고객관리 화면**으로 바로 이동하게 함. "저장만 하고 나가기"는 "저장"으로 라벨만 단순화(동작은 그대로 — 저장 후 고객목록). **의견**: 상담결과 입력 자체를 없애자는 뜻으로도 읽혔지만, 이 결과값이 고객목록 배지·자동 상태전환(성공→'계약')에 쓰이고 있어 그대로 유지하고 "저장 후 어디로 갈지"만 고객관리로 바꿈 — 이렇게 하면 상담 끝나자마자 바로 그 고객 히스토리·Q&A로 이어져서 요청하신 취지에 더 맞다고 판단함. `ap.js`.
6. 전체 `node --check` 통과 확인.

## 앱15 세션 이어서 (2026-09-06) — 홈화면 개편
1. **상단 배너 확대** — 마스코트 이미지(72→94px), 제목 글자(23→28px), 배너 여백 키움. 버전 표시도 v40(2026-08-17)→v41(2026-09-06)로 갱신.
2. **중간 콘텐츠 재정렬** — 예전엔 "PC 전체 백업" 버튼이 맨 위, "시작하기" 카드가 그 아래였음. 순서를 바꿔서 "시작하기"(메인 CTA)를 맨 위로 올리고, PC 백업 버튼은 저장소관리/시스템셋업 카드 아래(보조 기능 자리)로 내림.
3. **저장소 관리 / 시스템 셋업 업데이트** — 저장소 관리 카드 설명에 "음원" 추가(고객관리 음원도 여기서 관리됨을 명시). 시스템 셋업(관리자 설정 가이드)에 이번에 완료한 **구글 드라이브 연동** 항목(F: 발급값 위치, G: 문제 생겼을 때 확인할 것들) 새로 추가 — 예전엔 이 가이드에 구글 드라이브 얘기가 전혀 없었음.
- `index.html`, `style.css`, `pools.js`(renderAdminGuide).

## 앱15 세션 이어서 (2026-09-06) — 잔액 위젯 + 크레딧 라벨 정리
1. **"남은 잔액(추정)" 위젯에 콘솔 바로가기 버튼 추가** — 관리자 화면(담당자 관리/TA 관리/참조풀 관리 상단)의 잔액 카드에 "💳 콘솔에서 확인" 버튼을 넣어서 `platform.claude.com/settings/billing`로 바로 이동 가능. `analysis.js renderCostMeter()`.
2. **TA 자료 정리 시 라벨 버그 수정** — `organizeAdminPool()`에서 TA(poolType `'ta'`)를 깜빡하고 안 챙겨서, TA 자료를 정리해도 항상 "상담사례"로 잘못 표시되던 문제(AI에게 보내는 종류 설명도 틀렸었음). `renderAdminPoolEditorHtml()`과 같은 방식으로 맞춤. `advisor.js`.
3. **크레딧 사용 히스토리 라벨 세분화** — 예전엔 참조풀/TA/아이스브레이크/에피소드/카달로그 전부 뭉뚱그려 "참조풀 정리"로만 기록됐음. 이제 자료 종류별로 "TA 자료 정리", "상담사례 정리" 등으로 나눠 기록 → 잔액 카드의 "사용 히스토리 → 종류별 합계"에서 어디에 크레딧을 많이 쓰는지 구분해서 볼 수 있음. `advisor.js aiOrganizePool()`.
- 기존에 이미 쌓인 사용 기록은 그대로 "참조풀 정리"로 남고, 이후 기록부터 세분화됨(마이그레이션 없음).

## ⚠️ 구글 드라이브 "가져오기" 403 오류(access_denied) — jam님이 직접 해야 하는 설정
2026-09-06 실사용 중 발생: 고객관리 → "☁️ 구글드라이브에서 가져오기" 눌렀더니 구글이 "액세스 차단됨: jam-customer-console.pages.dev은(는) Google 인증 절차를 완료하지 않았습니다" 403 오류를 보여줌.
- **원인**: OAuth 앱이 아직 "테스트" 상태라(공식 게시 전), Google Cloud Console의 OAuth 동의 화면에 **테스트 사용자로 등록된 구글 계정만** 로그인할 수 있음. jaemyoung63@gmail.com이 아직 그 목록에 없어서 막힘. (코드 문제 아님 — Cloud Console 설정만 하면 됨.)
- **해결 방법**: ① console.cloud.google.com/apis/credentials/consent 접속(구글드라이브 연동 만들 때 쓴 그 프로젝트) → ② "테스트 사용자" 섹션 → ③ "+ ADD USERS" → jaemyoung63@gmail.com(그리고 앞으로 이 기능 쓸 다른 담당자 계정도) 추가 → ④ 저장 → ⑤ 앱에서 다시 "☁️ 구글드라이브에서 가져오기" 시도.
- 앱 안 "⚙ 시스템 셋업 → 관리자 설정" 가이드 맨 아래 G단계에도 이 내용 적어뒀음(2026-09-06 세션에서 추가).
- 테스트 상태는 최대 100명 테스트 사용자까지 무료로 쓸 수 있어서, 담당자가 100명을 넘지 않는 한 "게시(Publish)"까지는 필요 없음.

## 앱15 세션 이어서 (2026-09-06) — 고객관리 히스토리 카드 정리
- 카드 한 장이 제목줄+요약줄+날짜줄로 3~4줄씩 차지해서 목록이 길어 보였음 → "날짜+제목" 한 줄, "요약 미리보기" 한 줄, 딱 두 줄로 압축(길면 말줄임표). 클릭하면 기존과 동일하게 상세 내용 창(openSubPage)이 뜸 — 이 부분은 이미 그렇게 돼 있었음.
- "Q&A 물어보기" 버튼을 히스토리 목록 위 → 아래로 이동.
- Q&A 버튼을 눌렀을 때도 상세보기와 같은 방식(openSubPage)으로 창이 뜨는데, 이게 휴대폰(작은 화면)에서는 이미 전체화면처럼 나옴 — PC 브라우저에서는 화면 하단에서 올라오는 시트 형태로 보임(의도된 동작). `customermgmt.js`.

## 앱15 세션 이어서 (2026-09-06) — 히스토리에 "반응·관심도" 필드 추가
jam님 아이디어: 상담 음원에는 말투·어조로 고객의 관심도·반응을 읽을 수 있는 정보가 많이 담겨 있다 → 이걸 고객관리 Q&A에서 활용할 수 없을까.
- **확인한 사실**: Claude API는 2026-09 현재도 음성을 직접 듣는 기능이 없음(텍스트+사진만 입력 가능, 오디오 입력은 아직 미출시 기능 요청 단계) — 그래서 "AI가 음원을 직접 듣고 톤을 분석"하는 건 지금은 불가능. 다만 Whisper 받아쓰기 텍스트 자체에는 어조 단서(망설임 표현·반복 질문·즉각 반응 등)가 어느 정도 남아있어서, 이걸 AI가 읽어내도록 프롬프트만 개선하는 방법을 택함(jam님이 3가지 방법 중 "AI 프롬프트 개선"을 선택).
- **적용 내용**: `organize_history` 프롬프트(`functions/_lib/prompts.js`)에 `reaction` 필드 추가 — 어조 단서로 읽은 관심도·감정 신호를 한 문장으로 뽑아냄(근거 없으면 "판단 어려움"). 히스토리 항목에 `reaction` 필드로 저장(`customermgmt.js`), 상세보기 화면 맨 위에 "🎯 반응·관심도" 배지로 표시, Q&A에 보내는 컨텍스트(`buildCmHistoryText`)에도 포함해서 "반응이 어땠어?" 같은 질문에 답할 수 있게 함. Q&A 프롬프트(`history_qna`)에도 이 반응 줄을 적극 활용하라고 안내 추가.
- 목록 카드(2줄 압축)에는 반응을 안 넣음 — 지난 요청대로 카드는 간결하게 유지, 반응은 상세보기·Q&A에서만 노출.
- 추가 비용 없음(같은 AI 호출 안에서 필드 하나 더 받는 것뿐). 기존에 이미 저장된 히스토리는 reaction 필드가 없어 빈 값으로 처리됨(새로 정리하는 기록부터 적용).

## 앱15 세션 이어서 (2026-09-06) — 고객관리 입력·목록 사용성 개선
1. **텍스트도 폴더에서 선택·구글드라이브에서 가져오기 가능** — 예전엔 새 기록 텍스트 칸이 직접 타이핑이나 드래그&드롭만 됐음. 음원 칸처럼 "📁 폴더에서 선택"·"☁️ 구글드라이브에서 가져오기" 버튼을 추가함. 구글드라이브 연동은 원래 음원 전용으로 짜여 있던 걸 `_gpDriveTarget`(audio/text)로 일반화해서 텍스트도 같은 흐름으로 가져오게 함. `customermgmt.js`.
2. **음원 20MB 초과 시 텍스트만이라도 자동 진행** — 예전엔 음원이 크면(20MB↑) 같이 넣어둔 텍스트가 있어도 통째로 멈췄음. 이제 텍스트가 있으면 "음원은 빼고 텍스트만 정리할까요?" 확인 후 텍스트만으로 진행 가능(jam님이 3개 방법 중 이 방식 선택).
3. **히스토리 카드에 바로 삭제(✕) 버튼 추가** — 예전엔 상세보기 화면까지 들어가야 삭제할 수 있었음. 이제 목록 카드 우측 상단 ✕로 바로 삭제 가능(상세보기 안 삭제 버튼도 그대로 유지).

## 참고: 히스토리에 나오는 "다음 일정"이 뭔지
AP(상담) 화면에서 상담을 마치고 "상담결과" 팝업(성공/보류/실패 + 다음 일정 + 메모)을 저장하면, 그 내용이 자동으로 고객관리 히스토리에 한 줄 추가됨(`ap.js saveConsultResult`). 이때 "다음 일정" 칸에 재상담·방문 예정일을 입력해두면, 히스토리 요약에 "다음 일정: YYYY-MM-DD"로 같이 남는 것 — jam님이 상담결과 저장할 때 직접 입력한 값이 그대로 보이는 것뿐, 시스템이 알아서 만든 일정은 아님.

## 앱15 세션 이어서 (2026-09-06) — 업로드 파일 목록 표시
jam님 피드백: 텍스트·음원 파일을 올려도 뭘 올렸는지 화면에 안 보임.
- **적용**: advisor.js 관리자 화면(참조풀·TA 자료 편집)과 같은 방식을 고객관리에도 적용. 텍스트 파일은 본문에 "━━━ 📄 파일명 ━━━" 구분선으로 표시되고, 새 기록 추가 칸 아래 칩(chip) 목록에 파일명이 뜬다. 음원도 같은 목록에 🎤 아이콘으로 같이 뜸. 각 칩의 ×로 그 파일만 개별 삭제 가능(텍스트는 본문에서 그 블록만 제거, 음원은 첨부 해제).
- `customermgmt.js`: `attachCmAudioFile`/`appendCmText`에 파일명 추적 추가, `renderCmFileList()`/`removeCmFileAt()` 신설.

## 참고: Q&A에 뜨는 "히스토리 기록 총 N건"은 제한이 아님
Q&A 창 상단에 뜨는 "총 N건만 근거로 답해요"는 인위적인 상한이 아니라, **그 고객에게 실제로 저장된 히스토리 기록 전체 개수**를 그대로 보여주는 것(`openCmQna()`의 `hist.length` = `c.history.length`). 기록이 5건이면 5건 전부 Q&A에 쓰임. 다만 내부적으로 한 번에 보낼 수 있는 글자 수 예산(6000자)이 있어서, 기록이 아주 많이 쌓이면(보통 수십 건 이상) 오래된 기록 일부가 빠질 수 있음 — 지금처럼 몇 건 수준에서는 해당 없음.

## 앱15 세션 이어서 (2026-09-06) — Q&A 답변을 "근거+다각도 제안" 구조로 개편
jam님 요청: 단순 사실 답변 말고, 어느 기록의 어떤 내용을 근거로 했는지 밝히고, 긍정적인 면·부정적인 면·고객 입장에서 생각·제안까지 개조식으로 다양하게 답해줬으면.
- **적용**: `history_qna` 프롬프트를 질문 성격에 따라 분기하도록 개편 — 단순 사실 질문("언제 통화했어?")은 기존처럼 짧은 문장형, 판단·전략을 묻는 질문("반응 어땠어?", "다음에 어떻게 접근하면 좋을까?")은 `[근거]`→`[긍정적인 면]`→`[우려되는 점]`→`[고객 입장에서 생각해보면]`→`[제안]` 순서의 대괄호 소제목+개조식(하이픈)으로 답하게 함. [근거]는 실제 기록의 날짜·제목·내용을 인용하도록 강제. 나머지 섹션은 "설계사의 판단을 돕는 제안"이지 확정 사실이 아니라는 점을 프롬프트에 명시해서, jam님이 AI 추정과 기록된 사실을 헷갈리지 않게 함.
- **화면 표시**: 새 답변 렌더링은 분석 화면(`analysis.js`)에서 이미 쓰던 `linesBlock()`을 그대로 재사용 — 대괄호 소제목은 굵게, 나머지는 "· " 불릿으로 자동 정리. 단순 문장형 답변(대괄호 없음)은 기존처럼 문단으로 그대로 보여줌(불릿 하나에 문단 전체 들어가는 어색함 방지).
- Q&A 창 상단 설명 문구·입력창 placeholder도 이 방향 질문 예시로 갱신.
- 추가 비용 없음(같은 AI 호출, 프롬프트만 개편).
- `functions/_lib/prompts.js`(historyQnaSystem), `public/js/customermgmt.js`(askCmQna, openCmQna).

## 앱15 세션 이어서 (2026-09-06) — Q&A를 팝업 창 → 전용 화면으로, 질문 기록도 누적
jam님 요청: Q&A도 질문·답변이 히스토리처럼 쌓였으면 좋겠고, 팝업 창 말고 화면으로 전환됐으면.
- **전용 화면 신설** — `#s-cmqna`(하단탭에는 안 나오는 화면, `go('cmqna')`로 이동)를 새로 만들고, "🤖 Q&A 물어보기" 버튼이 이제 팝업(openSubPage) 대신 이 화면으로 이동하게 바꿈. 화면 위쪽에 "‹ 고객관리로" 버튼으로 돌아갈 수 있음. `index.html`(`s-cmqna` 추가), `core.js`(`go()`에 라우팅 추가).
- **질문·답변 히스토리 누적** — 고객 데이터에 `qnaHistory` 배열을 새로 추가(`{id, at, question, answer}`). 질문할 때마다 저장되고, 화면 아래 "질문 기록 (N)"에 통화 히스토리와 같은 2줄 카드(날짜+질문 / 답변 미리보기)로 쌓여서 다시 볼 수 있음. 카드 클릭하면 전체 질문·답변 상세보기, 카드 우측 ✕로 개별 삭제도 가능(통화 히스토리 카드와 동일한 패턴).
- `customermgmt.js`: `renderCmQnaScreen()`/`renderCmQnaList()`/`cmQnaCard()`/`viewCmQna()`/`deleteCmQna()` 신설, `openCmQna()`는 화면 이동만 하도록 변경, `askCmQna()`는 답변 후 `qnaHistory`에 저장하도록 확장.

## ⚠️ 앱15 세션 이어서 (2026-09-06) — "고객 리스트가 사라졌다" 신고 — 원인 확인·수정 완료
**데이터 손실 아님.** 원인 확인: 고객 화면 위쪽 "고객 이름으로 찾기" 검색창에 "창원"을 입력했는데, 검색은 이름만 대상이라(주소·지역은 검색 안 됨) 일치하는 고객이 없어 "'창원' 이름의 고객이 없어요"라는 빈 화면이 뜬 것. 이 검색어(`custSearch`)는 화면을 나갔다 다시 들어와도 안 지워지는 전역 변수라서, 다른 탭 갔다가 고객 탭으로 돌아와도 계속 그 상태로 남아있었음 — 그래서 실제로는 참조풀·분석·상담·관리를 진행한 고객들이 전부 그대로 저장돼 있는데, 검색 필터에 걸려 하나도 안 보였던 것.
- **수정**: ① 검색창 안에 텍스트가 있을 때만 나타나는 ✕(지우기) 버튼 추가 — 누르면 바로 전체 목록으로. ② "OO 이름의 고객이 없어요" 화면에도 "등록된 다른 고객은 그대로 있어요 — 검색어 지우고 전체 보기" 링크를 눈에 띄게 추가해서, 이런 상황이 또 생겨도 바로 알아채고 해결할 수 있게 함. `customers.js`.

## 앱16 세션 (2026-09-07) — 참조풀 카탈로그 이미지 저장 안 되는 버그 수정
jam님 신고: 관리자화면 참조풀관리 → 새 카달로그에서 이미지를 드래그앤드롭했는데 저장이 안 되고 "없음"으로 뜸. 이미지·PDF 클릭해서 볼 수 있게, 사용자 화면에서도 보이게, 이미지에 X 삭제 버튼도 추가해달라는 요청.
- **원인**: 사진을 앱 안에서 축소·압축할 때(`canvas.toBlob`) 사진 크기가 너무 크거나 형식이 특이하면 브라우저가 변환에 실패해 `blob`이 `null`로 나올 수 있는데, 이 경우를 확인하지 않고 그대로 저장을 진행해서 "빈 사진"(blob 없음) 기록이 만들어졌음 — 그게 화면에서 "없음"으로 보인 것. 관리자 카달로그(`advisor.js`)뿐 아니라 같은 방식으로 짜여진 고객 사진 첨부(`customers.js`/`ocr.js`)와 가입설계서 사진(`pools.js`)에도 동일한 허점이 있어서 같이 고침.
- **수정**: ① `blob`이 비어있으면 저장하지 않고 "사진을 저장하지 못했습니다" 안내 후 중단하도록 3곳 모두에 안전장치 추가. ② 혹시 이미 "없음"으로 남아있는 깨진 항목을 손으로 지울 수 있게, 관리자 카달로그·가입설계서 사진 화면의 "없음" 표시에도 X 삭제 버튼 추가(고객 사진 화면엔 이미 있었음).
- **클릭해서 크게 보기·사용자 화면 노출**: 코드 확인 결과 이미 구현되어 있었음 — 관리자 편집화면에서 이미지를 누르면 확대(라이트박스)되고, X로 개별 삭제도 이미 가능했음. 담당자용 참조풀 화면(`viewPoolItem`)도 이미 이미지·PDF를 보여주고 클릭하면 확대되도록 되어 있었음. 다만 위 버그 때문에 이미지가 애초에 제대로 저장이 안 됐으니 아무것도 안 보였던 것 — 저장 버그를 고치면 이 두 기능은 그대로 작동함.
- `public/js/advisor.js`(addAdminPoolImageDirect, renderAdminPoolThumbs), `public/js/pools.js`(addPoolImageDirect, renderPoolThumbs), `public/js/ocr.js`(addImageDirect).

## 앱16 세션 이어서 (2026-09-07) — 참조풀 사진·음원이 다른 화면·기기에서 영원히 "아직 안 내려받아졌어요"였던 진짜 원인 발견·수정
jam님이 위 버그 수정 후 실제로 카달로그에 사진 4장+음원을 올렸는데, 참조풀(담당자 화면)에서 열어보니 사진 4장·음원 전부 "아직 안 내려받아졌어요"로만 뜸(스크린샷 확인).
- **원인**: 참조풀(아이스브레이크·상담사례·에피소드·카달로그·TA)은 2026-08-17부터 특정 담당자 개인 소유가 아니라 `owner='shared'`라는 공용 값으로 저장되도록 바뀌었는데, 정작 사진·음원 파일을 클라우드(R2)에 올리고 받아오는 서버 코드(`ownerBelongsToAdvisor`, `functions/_lib/advisors.js`)는 이 변경을 반영하지 못한 채 여전히 "그 자료의 owner가 지금 로그인한 담당자 id와 정확히 같아야 함"만 확인하고 있었음. `owner`가 항상 `'shared'`라는 문자열이니 어떤 담당자로 로그인해도 절대 일치할 수 없어서, **참조풀에 딸린 사진·음원은 애초에 R2에 올라간 적이 한 번도 없었음**(업로드 시도 자체가 매번 403으로 조용히 실패). 그래서 새로고침을 몇 번을 해도, 다른 기기에서 봐도 영원히 "아직 안 내려받아졌어요"만 뜬 것 — 다운로드할 원본이 서버에 아예 없었기 때문. (참조풀 텍스트 내용·태그 등은 이 문제와 무관한 별도 경로라 정상 작동해왔음)
- **수정**: `ownerBelongsToAdvisor`에서 `ownerType==='pool'`이고 `owner==='shared'`이면 모든 담당자에게 접근을 허용하도록 예외 추가. 이제 참조풀 사진·음원 업로드/다운로드가 정상적으로 통과됨.
- **jam님이 확인할 것**: 원래 이미지·음원을 올렸던 그 기기(브라우저)에서 — 아직 그 기기 로컬에는 사진·음원 실물이 남아있을 것이므로 — 홈 → **저장소 관리 → "클라우드 파일 동기화" 카드 → "지금 동기화" 버튼**을 눌러서 이번에 고친 서버로 다시 올려보내면 됨. 그 후 참조풀(담당자 화면)이나 다른 기기에서 열어보면 "아직 안 내려받아졌어요" 대신 실제 사진·음원이 보여야 함.
- `functions/_lib/advisors.js`(ownerBelongsToAdvisor).

## 앱16 세션 이어서 (2026-09-07) — 저장소 진단 "안 쓰는 이미지 삭제"가 방금 올린 참조풀 사진·음원을 지워버리던 문제 + 음원을 참고용 재생 전용으로 전환
jam님이 위 R2 권한 수정 후에도 여전히 문제가 있었음: 사용자 화면에서는 "아직 안 내려받아졌어요", 관리자 화면으로 돌아오면 이미지는 "없음", 음원은 첨부 자체가 사라짐.
- **원인**: 관리자에서 참조풀을 저장하면 서버(D1)에는 저장되지만, 이 기기의 로컬 IndexedDB "pools" 저장소에는 반영이 안 되고 있었음. 그런데 "저장소 관리 → 안 쓰는 이미지 정리"는 "고객·참조풀이 실제로 쓰는 이미지" 목록을 이 로컬 IndexedDB만 보고 계산하기 때문에, 방금 올린 사진·음원을 "어디서도 안 쓰는 이미지"로 잘못 판단해서 실제로 지워버렸을 가능성이 매우 높음(그래서 사진은 "없음", 음원은 첨부 자체가 사라진 것처럼 보임).
- **수정**: 관리자가 참조풀을 저장·삭제할 때 이 기기의 로컬 IndexedDB에도 즉시 반영하도록 수정 — 이제 저장 직후부터 "안 쓰는 이미지 정리"가 방금 올린 파일을 정확히 인식해서 더는 실수로 지우지 않음.
- **jam님이 확인할 것**: 카달로그(또는 다른 참조풀)에 사진·음원을 다시 한번 올리고 저장 → 바로 관리자 화면에서 다시 열어봐서 "없음" 없이 그대로 남아있는지 확인. 그다음 "지금 동기화"(홈 → 저장소 관리)로 클라우드에 올리고, 참조풀(담당자 화면)에서도 보이는지 확인.
- `public/js/advisor.js`(saveAdminPool, doAdminDeletePool).

jam님 요청: 음원을 "AI로 정리"에 쓰지 말고 참고용 재생 전용으로 바꿔달라, 긴 음원도 올릴 수 있게 해달라.
- **적용**: "AI로 정리"는 이제 ①번 텍스트 칸만 정리하고, 음원은 더 이상 Whisper로 받아쓰기해서 함께 정리하지 않음 — ④번 PDF·이미지처럼 "담당자 화면에서 재생만 되는 참고 자료"로 성격이 바뀜. 그 대신 Whisper 때문에 걸려 있던 20MB 제한을 없애서 긴 녹음도 올릴 수 있게 함(다만 60MB 넘으면 "클라우드 업로드가 오래 걸리거나 실패할 수 있다"는 안내만 하고 막지는 않음). 화면 안내 문구도 이에 맞게 전부 수정.
- 내용을 AI로 정리하고 싶으면 이제는 음원이 아니라 ①번 텍스트 칸에 직접 옮겨 적어야 함(휴대폰 녹음 앱 등으로 미리 글로 바꿔서).
- `public/js/advisor.js`(organizeAdminPool, attachAdminPoolAudioFile, renderAdminPoolEditorHtml, handleAdminPoolDropFile, aiOrganizePool 주변 안내문구).

## 앱16 세션 이어서 (2026-09-07) — "아까하고 동일해요" 신고 → 캐시버스터 누락 발견 + 음원 파일명 확인 안 되는 문제 수정
jam님이 위 수정들을 반영해서 다시 테스트했는데 "아까하고 동일해요(없음·동기화 안 됨·음원도 없음)"라고 신고. 코드는 이미 고쳤고 GitHub에도 이미 올라가 있는 상태였는데(원격 저장소 확인함) 그대로라고 하셔서, 배포/캐시 쪽 문제일 가능성이 높다고 보고 점검함.
- **발견**: `index.html`의 `<script src="js/advisor.js">`처럼 파일 이름에 버전이 안 붙어있어서, 브라우저가 예전에 받아둔 js 파일을 계속 재사용(캐시)할 수 있는 구조였음. 서버에는 새 코드가 올라가 있어도 jam님 브라우저가 옛날 파일을 계속 쓰고 있었을 가능성이 있음.
- **수정**: 모든 css/js 파일 주소 뒤에 `?v=44`를 붙여서, 버전이 바뀔 때마다 브라우저가 무조건 새로 받아오게 함(이제부터 버전 올릴 때 이 숫자도 같이 올려야 함 — 위 "핵심" 항목 5번 참고).
- **음원 파일명 표시 추가**(jam님 요청) — 관리자 화면에서 음원을 올리면 이제 재생 버튼 위에 "📎 파일명 · 용량"이 표시돼서 어떤 파일이 올라갔는지 바로 확인 가능. 또한 음원 참조는 있는데 이 기기에 실물이 없는 경우(다른 기기에서 올렸거나 지워진 경우) "＋ 음원 추가"로 조용히 넘어가지 않고 "⚠ 이 기기에 음원 실물이 없어요" 경고 + "이 음원 연결 지우기" 버튼을 보여주도록 바꿈(전에는 이 상태가 "음원이 아예 없던 것"처럼 보여서 혼란을 줬음).
- `public/index.html`(캐시버스터), `public/js/advisor.js`(renderAdminPoolAudio, attachAdminPoolAudioFile).
- **jam님이 확인할 것**: 브라우저를 완전히 새로고침(PC는 Ctrl+F5, 모바일은 앱을 완전히 껐다가 다시 켜기)한 뒤, 홈 화면 아래에 **v44**가 뜨는지부터 확인해주세요. v44가 안 뜨면 아직 새 버전을 못 받은 것이니 그 상태로는 무엇을 테스트해도 예전 그대로일 수밖에 없어요. v44 확인되면 그때 카달로그 사진·음원을 다시 테스트해주세요.

## 앱16 세션 이어서 (2026-09-07) — 진짜 최종 원인: 로그아웃 후 재로그인만 해도 로컬 사진·음원이 통째로 사라지던 버그 발견·수정
jam님이 계속 안 된다고 하셔서 워크플로우를 자세히 물어보니: "관리자화면에서 저장 후 홈으로 가려면 로그오프하고 다시 로그인해서 들어간다"고 하심. 이게 결정적 단서였음.
- **원인**: `advisorLogin()`(core.js)에 "다른 담당자가 이 기기를 쓰던 흔적이 있으면 로컬 자료를 비운다"는 안전장치가 있는데, "이전 담당자가 누구였는지"를 로그아웃하면 사라지는 값(`localStorage.advisorId`)으로 판단하고 있었음. 그래서 **로그아웃했다가 완전히 같은 담당자로 바로 다시 로그인만 해도** "새 기기(또는 다른 사람)"로 착각해서 로컬 저장소(고객·참조풀·사진·음원 전부)를 통째로 비워버렸음 — 그 안에 아직 클라우드에 못 올라간, 방금 관리자 화면에서 올린 사진·음원이 있었다면 그대로 증발. 몇 번을 다시 올려도 로그아웃→재로그인 과정에서 계속 지워졌으니 영원히 "없음"으로 보인 것 — **이번이 진짜 최종 원인.**
- **수정**: "이전 담당자"를 로그아웃해도 지워지지 않는 별도 값(`lastLocalAdvisorId`)으로 추적하도록 변경. 이제 같은 담당자가 로그아웃 후 다시 들어오는 건 안전하게 로컬 자료를 보존하고, 실제로 다른 담당자로 전환할 때(담당자 전환 기능)만 로컬을 비움.
- **추가로 중요한 점**: 사실 관리자 화면은 로그아웃 안 해도 나갈 수 있음 — 관리자 화면 맨 위(위로 스크롤하면 보임)에 **"닫기"** 버튼이 있고, 이걸 누르면 로그아웃 없이 바로 원래 화면(홈 등)으로 돌아감. 앞으로는 로그아웃 대신 이 "닫기"를 쓰면 이런 위험을 아예 피할 수 있음.
- `public/js/core.js`(advisorLogin, advisorSwitch).
- **jam님이 확인할 것**: 이제부터는 관리자 화면에서 나갈 때 로그아웃하지 말고 위로 스크롤해서 **"닫기"** 버튼을 눌러주세요(취소로 편집만 닫고, 닫기로 관리자 화면 전체를 나가면 됨). 그 상태로 카달로그에 사진·음원 다시 올리기 → 저장 → 저장소 관리(로그아웃 안 하고 홈에서 바로 이동) → 지금 동기화 → 참조풀에서 확인. 이번엔 정상 작동해야 합니다.

## 앱16 세션 이어서 (2026-09-07) — 확인 완료! 사용자 화면에도 음원 파일명 표시 추가
jam님 확인: 위 로그아웃 버그 수정 후 드디어 사진·음원이 정상적으로 뜸. 추가 요청: 관리자 화면처럼 사용자(참조풀) 화면에서도 음원 파일명이 보이면 좋겠다.
- **적용**: `viewPoolItem()`(참조풀 상세보기)의 음원 재생 영역 위에도 "📎 파일명 · 용량"을 표시하도록 추가(관리자 편집화면과 동일한 표시 방식).
- `public/js/pools.js`(viewPoolItem).

## 아직 남은 것 / 확인 필요
- **로그아웃 없이 재테스트** — 위 방법대로(관리자 화면은 "닫기"로만 나가기) 사진·음원을 다시 올려서 최종 확인.
- **v44 배포 확인** — jam님 화면에 v44가 뜨는지 최우선 확인. 안 뜨면 아직 배포/캐시 문제이니 다른 테스트보다 이것부터 해결.
- **참조풀 저장 직후 로컬 반영 확인** — 위 방법대로 사진·음원 다시 올려서 저장 후 관리자 화면에서 바로 다시 열어봤을 때 "없음" 없이 남아있는지, "지금 동기화" 후 담당자 화면에서도 보이는지 확인.
- **음원 참고용 전환 확인** — 참조풀 편집에서 긴 음원(예: 10분 이상 통화 녹음)을 올려도 막히지 않는지, "AI로 정리"를 눌렀을 때 음원 내용이 더 이상 텍스트에 섞여 들어오지 않고 텍스트만 정리되는지, 음원은 재생만 되는지 확인.
- **참조풀 사진·음원 재동기화 확인** — 위 방법대로 "지금 동기화" 누른 뒤, 카달로그(또는 그 사진을 올렸던 항목)를 다른 화면·다른 기기에서 열어서 사진·음원이 실제로 뜨는지 확인.
- **카달로그 이미지 저장 확인** — Push 후 참조풀관리 → 새 카달로그(또는 다른 종류)에서 사진 드래그앤드롭 → "없음" 없이 정상적으로 사진이 뜨는지, 클릭하면 확대되는지, X로 삭제되는지, 저장 후 담당자용 참조풀 화면에서도 보이는지 확인. (원래 갖고 있던 큰 사진으로도 한 번 더 테스트 권장 — 이번 버그가 큰 사진에서 주로 생겼을 가능성이 있음)
- **검색어 지우기 버튼 실사용 확인** — 고객 검색창에 아무 이름이나 입력해보고 ✕ 버튼·"검색어 지우고 전체 보기" 링크가 잘 뜨고 눌렀을 때 전체 고객이 다시 보이는지 확인.
- **Q&A 전용 화면·질문 기록 실사용 확인** — Q&A 버튼 눌렀을 때 팝업이 아니라 화면으로 전환되는지, "‹ 고객관리로" 버튼이 잘 되는지, 질문할 때마다 아래 "질문 기록"에 쌓이는지, 카드 클릭·삭제가 잘 되는지 확인.
- **Q&A 새 답변 구조 실사용 확인** — "이 고객 다음에 어떻게 접근하면 좋을까?" 같은 전략성 질문을 실제로 물어봐서 [근거]/[긍정적인 면]/[우려되는 점]/[고객 입장]/[제안] 구조로 잘 나오는지, 근거가 실제 기록 내용과 맞는지 확인.
- **업로드 파일 목록 실사용 확인** — 텍스트·음원을 여러 개 넣어보고 칩 목록에 다 뜨는지, ×로 개별 삭제가 잘 되는지 확인.
- **반응·관심도 필드 실사용 확인** — 새로 음원/텍스트를 정리해서 히스토리에 추가할 때 "🎯 반응·관심도" 배지가 상세보기에 뜨는지, Q&A에서 "반응 어땠어?" 물어봤을 때 이 내용을 활용해 답하는지 확인.
- **구글 드라이브 테스트 사용자 등록** — 위 방법대로 jam님이 Cloud Console에서 직접 등록 필요(코드로 해결 불가). 등록 후 재시도 확인.
- **홈화면 개편 확인** — Push 후 홈화면에서 상단 배너가 커졌는지, "시작하기" 카드가 맨 위로 왔는지, ⚙시스템 셋업 → 관리자 설정 → 맨 아래 새로 추가된 구글드라이브 항목이 잘 보이는지 확인.
- **위 6건 실사용 테스트 필요** — Push 후 ①홈 버튼 크기 확인, ②홈 "시작하기"→TA로 가는지, ③TA 화면 "다음" 버튼→고객정보 이동, ④상담사례풀에서 기존 자료들이 상품별로 잘 나뉘는지(미분류가 줄었는지), ⑤AP 상담결과 저장 후 고객관리 화면으로 잘 넘어가는지·그 고객이 자동 선택되는지 확인.
- **6탭 개편(TA·고객관리·구글드라이브·Q&A) 실사용 테스트 필요** — Push 후 ①⚙ 관리자 화면 → TA 관리에서 자료 등록·공개 체크 → 담당자 화면 TA 탭 확인, ②고객관리 탭에서 메모/음원 넣고 AI 정리 → 히스토리에 잘 쌓이는지, ③AP 상담결과 저장 후 고객관리에 자동으로 기록이 남는지, ④"☁️ 구글드라이브에서 가져오기"로 실제 음원 파일 가져와보기, ⑤Q&A 버튼으로 실제 질문 몇 개 해보고 답변이 기록 내용과 맞는지·비용이 얼마나 드는지 확인.
- (참고, 할 일 아님) 테스트로 만든 고객 **"테스트고객"**(id `mtbnuaiasw995`) — jam님이 필요할 때 직접 삭제.
